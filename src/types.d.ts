declare namespace App {
    interface Locals {
        username?: string;
        failure?: object;
        // roles?: string[];
    }

    // interface Error {
    //     code: string;
    //     id: string;
    //     message: string = 'Invalid password';
    // }
}